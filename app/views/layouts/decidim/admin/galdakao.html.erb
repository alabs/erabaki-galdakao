<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Galdakao · <%= content_for?(:title) ? yield(:title) : "Decidim Admin" %></title>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= snippets.for(:head) if respond_to?(:snippets) %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
